# All evaluator code is vendored under ``distill_v2_eval/``; the
# environment has no runtime dependency on the upstream project.
# Tokenizer + wire layer only — no torch (forward is delegated to the
# caller's vLLM endpoint), no SQLAlchemy/asyncpg (rollouts come from a
# parquet URL on R2, not PG).
httpx>=0.27
# Pinned: ``>=`` let build-time version drift to a release whose lazy
# ``from transformers import AutoTokenizer`` failed at runtime (ImportError),
# which scored every distill-v2 cell INVALID in ~1-2s. Pin so the image is
# reproducible.
transformers==5.9.0
sentencepiece>=0.2
pydantic>=2.7
pydantic-settings>=2.3
zstandard>=0.22
structlog>=24.1
orjson>=3.10
pyarrow>=15.0
pyyaml>=6.0
# Authenticated S3 reads of the private staging bucket (opt-in via R2_STAGING_*;
# see parquet_source._R2Staging). Inert without those env vars.
boto3>=1.34
